import { useEffect, useState } from "react";
import { Menu, X, Phone, ClipboardList } from "lucide-react";
import { Link } from "@tanstack/react-router";
import logo from "@/assets/livefit-logo.jpg.asset.json";
import { trackPhoneConversion } from "@/lib/conversion";
import { trackLeadEvent } from "@/lib/leadTracking";
import { NAV, PHONE_DISPLAY, PHONE_TEL } from "./data";
export function Header(){const [open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>12);onScroll();window.addEventListener("scroll",onScroll,{passive:true});return()=>window.removeEventListener("scroll",onScroll)},[]);const call=async()=>{await trackLeadEvent("call_click_direct",window.location.pathname,"header_call");trackPhoneConversion();window.location.href=PHONE_TEL};const book=async()=>{await trackLeadEvent("assessment_cta_click",window.location.pathname,"header_booking");window.location.href="/book-assessment"};return <header className={`sticky top-0 z-50 border-b transition-colors duration-300 ${scrolled?"border-border bg-background/90 backdrop-blur-md":"border-transparent bg-background"}`}><div className="mx-auto grid max-w-7xl grid-cols-[minmax(0,1fr)_auto] items-center gap-4 px-5 py-3.5 sm:px-8 lg:grid-cols-[auto_1fr_auto]"><Link to="/" className="flex min-w-0 items-center gap-3" onClick={()=>setOpen(false)}><img src={logo.url} alt="LiveFit Physiotherapy logo" width={44} height={44} className="h-10 w-10 shrink-0 rounded-sm object-cover mix-blend-multiply sm:h-11 sm:w-11"/><span className="min-w-0"><span className="block font-display text-lg leading-none tracking-tight sm:text-xl">LiveFit</span><span className="block eyebrow mt-1 text-[0.6rem] text-muted-foreground">Physiotherapy</span></span></Link><nav className="hidden justify-center gap-7 lg:flex">{NAV.map(item=><Link key={item.href} to={item.href} className="text-sm text-muted-foreground transition-colors hover:text-foreground">{item.label}</Link>)}</nav><div className="flex shrink-0 items-center gap-2"><button type="button" data-track-lead="handled" onClick={book} className="hidden items-center gap-2 rounded-full border border-charcoal/20 bg-card px-4 py-2.5 text-sm font-semibold text-charcoal sm:inline-flex"><ClipboardList className="h-3.5 w-3.5"/>Book Assessment · PKR 1,499</button><button type="button" data-track-lead="handled" onClick={call} className="inline-flex items-center gap-2 rounded-full bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground sm:px-6"><Phone className="h-3.5 w-3.5"/><span className="hidden sm:inline">Call Now — {PHONE_DISPLAY}</span><span className="sm:hidden">Call</span></button><button type="button" aria-label={open?"Close menu":"Open menu"} onClick={()=>setOpen(v=>!v)} className="grid h-10 w-10 shrink-0 place-items-center rounded-full border border-border lg:hidden">{open?<X className="h-4 w-4"/>:<Menu className="h-4 w-4"/>}</button></div></div>{open&&<nav className="border-t border-border bg-background lg:hidden"><div className="mx-auto flex max-w-7xl flex-col px-5 py-2 sm:px-8"><button type="button" onClick={()=>{setOpen(false);void book()}} className="border-b border-border/60 py-3.5 text-left text-sm font-semibold text-primary">Book Assessment · PKR 1,499</button>{NAV.map(item=><Link key={item.href} to={item.href} onClick={()=>setOpen(false)} className="border-b border-border/60 py-3.5 text-sm text-foreground last:border-0">{item.label}</Link>)}</div></nav>}</header>}
